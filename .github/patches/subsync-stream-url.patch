diff --git a/app/src/main/java/com/nuvio/tv/data/repository/SubtitleRepositoryImpl.kt b/app/src/main/java/com/nuvio/tv/data/repository/SubtitleRepositoryImpl.kt
index be4eea1..249a577 100644
--- a/app/src/main/java/com/nuvio/tv/data/repository/SubtitleRepositoryImpl.kt
+++ b/app/src/main/java/com/nuvio/tv/data/repository/SubtitleRepositoryImpl.kt
@@ -31,6 +31,7 @@ class SubtitleRepositoryImpl @Inject constructor(
     companion object {
         private const val TAG = "SubtitleRepository"
         private const val PER_ADDON_TIMEOUT_MS = 20_000L
+        private const val SUBSYNC_HOST = "subsync.kissmissi.duckdns.org"
     }
 
     override suspend fun getSubtitles(
@@ -40,6 +41,7 @@ class SubtitleRepositoryImpl @Inject constructor(
         videoHash: String?,
         videoSize: Long?,
         filename: String?,
+        streamUrl: String?,
         onProgress: ((completed: Int, total: Int, addonName: String?) -> Unit)?,
         onSubtitlesEmitted: ((List<Subtitle>) -> Unit)?
     ): List<Subtitle> = withContext(Dispatchers.IO) {
@@ -81,7 +83,7 @@ class SubtitleRepositoryImpl @Inject constructor(
                     val addonStartMs = System.currentTimeMillis()
                     val subtitles = try {
                         withTimeoutOrNull(PER_ADDON_TIMEOUT_MS) {
-                            fetchSubtitlesFromAddon(addon, type, id, videoId, videoHash, videoSize, filename)
+                            fetchSubtitlesFromAddon(addon, type, id, videoId, videoHash, videoSize, filename, streamUrl)
                         }
                     } catch (e: CancellationException) {
                         throw e
@@ -160,7 +162,8 @@ class SubtitleRepositoryImpl @Inject constructor(
         videoId: String?,
         videoHash: String?,
         videoSize: Long?,
-        filename: String?
+        filename: String?,
+        streamUrl: String?
     ): List<Subtitle> {
         val normalizedType = canonicalSubtitleType(type)
         val actualId: String = if (normalizedType == "series" && !videoId.isNullOrEmpty()) {
@@ -177,7 +180,7 @@ class SubtitleRepositoryImpl @Inject constructor(
         val queryStart = rawBaseUrl.indexOf('?')
         val basePath = if (queryStart >= 0) rawBaseUrl.substring(0, queryStart).trimEnd('/') else rawBaseUrl
         val baseQuery = if (queryStart >= 0) rawBaseUrl.substring(queryStart) else ""
-        val extraParams = buildExtraParams(videoHash, videoSize, filename)
+        val extraParams = buildExtraParams(addon, videoHash, videoSize, filename, streamUrl)
         val subtitleUrl = if (extraParams.isNotEmpty()) {
             "$basePath/subtitles/$encodedType/$encodedActualId/$extraParams.json$baseQuery"
         } else {
@@ -219,17 +222,25 @@ class SubtitleRepositoryImpl @Inject constructor(
     }
     
     private fun buildExtraParams(
+        addon: Addon,
         videoHash: String?,
         videoSize: Long?,
-        filename: String?
+        filename: String?,
+        streamUrl: String?
     ): String {
         val params = mutableListOf<String>()
-        
+
         videoHash?.let { params.add("videoHash=$it") }
         videoSize?.let { params.add("videoSize=$it") }
         filename?.let {
             params.add("filename=${encodePathSegment(it)}")
         }
+        // SubSync tier-2: the resolved stream URL lets the addon re-time subtitles
+        // against the stream audio. Sent ONLY to our own addon host — stream URLs
+        // embed debrid credentials and must never reach third-party addons.
+        streamUrl
+            ?.takeIf { it.startsWith("http") && addon.baseUrl.contains(SUBSYNC_HOST) }
+            ?.let { params.add("streamUrl=${encodePathSegment(it)}") }
         
         return if (params.isNotEmpty()) {
             params.joinToString("&")
diff --git a/app/src/main/java/com/nuvio/tv/domain/repository/SubtitleRepository.kt b/app/src/main/java/com/nuvio/tv/domain/repository/SubtitleRepository.kt
index d3b5b6e..a9cc4bb 100644
--- a/app/src/main/java/com/nuvio/tv/domain/repository/SubtitleRepository.kt
+++ b/app/src/main/java/com/nuvio/tv/domain/repository/SubtitleRepository.kt
@@ -11,6 +11,7 @@ interface SubtitleRepository {
      * @param videoHash Optional OpenSubtitles file hash
      * @param videoSize Optional video file size in bytes
      * @param filename Optional video filename
+     * @param streamUrl Optional resolved playback URL (sent ONLY to the SubSync addon)
      * @return List of subtitles from all addons
      */
     suspend fun getSubtitles(
@@ -20,6 +21,7 @@ interface SubtitleRepository {
         videoHash: String? = null,
         videoSize: Long? = null,
         filename: String? = null,
+        streamUrl: String? = null,
         onProgress: ((completed: Int, total: Int, addonName: String?) -> Unit)? = null,
         onSubtitlesEmitted: ((List<Subtitle>) -> Unit)? = null
     ): List<Subtitle>
diff --git a/app/src/main/java/com/nuvio/tv/ui/screens/player/PlayerRuntimeControllerObservers.kt b/app/src/main/java/com/nuvio/tv/ui/screens/player/PlayerRuntimeControllerObservers.kt
index fa7bc18..eaf226a 100644
--- a/app/src/main/java/com/nuvio/tv/ui/screens/player/PlayerRuntimeControllerObservers.kt
+++ b/app/src/main/java/com/nuvio/tv/ui/screens/player/PlayerRuntimeControllerObservers.kt
@@ -101,6 +101,7 @@ internal suspend fun PlayerRuntimeController.fetchAddonSubtitlesNow(
             videoHash = currentVideoHash,
             videoSize = currentVideoSize,
             filename = currentFilename,
+            streamUrl = currentStreamUrl.takeIf { it.startsWith("http") },
             onProgress = onProgress,
             onSubtitlesEmitted = { currentList ->
                 onSubtitlesEmitted?.invoke(withStreamSidecarSubtitles(currentList))
diff --git a/app/src/main/java/com/nuvio/tv/ui/screens/stream/StreamScreenViewModel.kt b/app/src/main/java/com/nuvio/tv/ui/screens/stream/StreamScreenViewModel.kt
index 76fe7aa..2b97eb0 100644
--- a/app/src/main/java/com/nuvio/tv/ui/screens/stream/StreamScreenViewModel.kt
+++ b/app/src/main/java/com/nuvio/tv/ui/screens/stream/StreamScreenViewModel.kt
@@ -1734,6 +1734,7 @@ class StreamScreenViewModel @Inject constructor(
                 videoHash = playbackInfo.videoHash,
                 videoSize = playbackInfo.videoSize,
                 filename = playbackInfo.filename,
+                streamUrl = playbackInfo.url?.takeIf { it.startsWith("http") },
                 onProgress = { completed, total, addonName ->
                     val msg = if (completed == 0) {
                         context.getString(R.string.player_loading_subtitles_from, total)
